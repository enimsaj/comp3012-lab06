<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/style.css">
  <title>Movieflix</title>
</head>

<body>
  <!-- Your Code Goes Here -->
  <div class="container todo-list">
    <% if(movie !== undefined){ %>
      <h1><%= movie.name %></h1>
      <p class="todo-list__label"><%= movie.description %></p>
      <a href="/">Back to home page</p>
    <% } else{ %>  
      <h1>Movie could not be found</h1>
   <% } %>
  </div>
</body>

</html>